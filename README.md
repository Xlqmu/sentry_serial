# 思路

## 联盟赛版本

现在的思路就是模式切换+定点
但是现在卡在模式切换的逻辑上，定点的逻辑似乎没有什么问题
主要逻辑是通过从下位机处理的topic数据来判断是否需要去执行什么任务，
如一但从topic相关数据中读到比赛状态为4时开始导航去中心点，通过从topic读到的数据时刻检测自身血量，一旦残血就去起始点回血，然后血回满就去中心点，到达中心点(导航任务完成后)就开启自瞄模式（发给下位机的数据包中的move_mode设为2），其余导航时设置为4

## 区域赛版本

### 前言

由于区域赛场地较为复杂，所以原有的决策无法支持现有的比赛形式，但是由于赛前并没有打算去采用行为树的方式去写决策，所以只能写这种丑陋的状态机了（实际上是我比较懒，而且实在是前期没有搞懂bt的逻辑，等后面搞懂的时候已经来不及去写了2333）

### 取舍

由于原先的串口部分代码并没有什么大的问题甚至说我觉得数据的处理方式也是还算勉强可以的，所以可能并不会优化串口通信部分，只会去根据实际的需要去更改串口通信的数据包和决策逻辑，而且决定给代码优化成类并且封装好

将数据包定义在头文件里面吧

### 决策思路

由于哨兵机器人的结构限制，使得其无法过半场前往对方半场，所以其导航逻辑减少了很大的工作量(bushi),只需要前期的时候掩护英雄去推掉前哨站，如果识别到己方前哨站掉了或者其他事件的话就回到其他巡逻点

* 关于堡垒

  由于无法在小yaw转动的时候去控制其底盘是正对上坡，可能会导致翻车等意外发生故初步放弃上堡垒

  有个想法：在哨兵前往堡垒点位的时候由于是底盘跟随云台的(先测这个，至少从上位机测试的结果来说是没有问题的)，所以只要不让云台转就行似乎

  不对，正方向那个轮子方位不对，后面问问电控看能不能实现

### 串口数据包定义

#### RxPacket(接收) 可能会增加到62个？

``` cpp
    struct __attribute__((packed)) RxPacket {
        uint8_t decator_mode;
        float roll;
        float pitch_gimbal;
        float yaw_gimbal;
        float pitch_chassis;
        float yaw_chassis;
        uint8_t game_status;
        uint16_t remaining_time;
        uint16_t blood;
        bool is_free_rebirth; // is_rfid
    };
```

##### 需要增加的信息

0x0003 所有的血量信息 32
0x0101 涉及到场地事件 4
0x020D 涉及到哨兵是否免费复活 6

#### TXPacket(发送)

``` cpp

    struct __attribute__((packed)) TxPacket {
        float pitch_diff;
        float yaw_diff;
        float distance;
        uint32_t fire_advice;
        float vx;
        float vy;
        float vw;
        uint32_t move_mode;
    };

```
### 血量监测和复活监测

设计一个顶层的监测器，从下位机去读取数据并采取顶层决策

#### 血量监测

#### 复活监测
